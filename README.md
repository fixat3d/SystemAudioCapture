The filetype is wav, wav seems to be the easiest to work with from a programming standpoint.
AUDIO ONLY, this does not caputure any video. Although ffmpeg is capable of capturing video.

This will of course need ffmpeg to be installed

Many ways to do that:
https://ffmpeg.org/download.html

windows cmd:
winget install ffmpeg

linux debian based:
apt install ffmpeg
